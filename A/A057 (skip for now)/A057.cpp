#include <iostream>
#include <vector>
using namespace std;

int main() {
    vector<int> cookie = {1,1,2,3};

    if (cookie.size() == 1) return 0; 
    if (cookie.size() == 2) { 
        if (cookie[0] == cookie[1]) return cookie[0];
        return 0; 
    }
    int mid = 0;
    int bro = 0;
    int lilbro = 0;
    while (true) {
        for(int i = mid; i >= 0;)
    }
    


}